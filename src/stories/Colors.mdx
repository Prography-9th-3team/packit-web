import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { colors } from 'styles/theme';

<Meta title='Colors' />

<ColorPalette>
  {Object.keys(colors).map((item) => {
    return <ColorItem key={item} title={item} subtitle={item + ' color'} colors={colors[item]} />;
  })}
</ColorPalette>
